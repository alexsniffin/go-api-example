apiVersion: v1
kind: Service
metadata:
  name: {{ template "go-api-example.fullname" . }}
  labels:
    app: {{ template "go-api-example.name" . }}
    chart: {{ template "go-api-example.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "go-api-example.name" . }}
    release: {{ .Release.Name }}
